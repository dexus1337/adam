add_subdirectory(adam)
if(NOT ANDROID)
    add_subdirectory(adam-cli)
endif()

if(NOT DEFINED ADAM_NO_GUI)
    add_subdirectory(adam-gui)
    if(NOT ANDROID)
        add_subdirectory(adam-cop)
    endif()
endif()